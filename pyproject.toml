[project]
name = "portfolio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "34syH", email = "ljh1001018395@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.2",
]

[project.scripts]
portfolio = "portfolio:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
